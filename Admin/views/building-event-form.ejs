<!DOCTYPE html>
<html>
<head>
    <title>Create Building Event</title>
    <style>
        body { font-family: Arial, sans-serif; max-width: 500px; margin: 0 auto; padding: 20px; }
        .form-group { margin-bottom: 15px; }
        label { display: block; margin-bottom: 5px; }
        input, select { width: 100%; padding: 8px; box-sizing: border-box; }
        button { background: #4285F4; color: white; border: none; padding: 10px 15px; cursor: pointer; }
        .error { color: red; margin-top: 10px; }
        .success { color: green; margin-top: 10px; }
        .readonly-field { background-color: #f5f5f5; color: #666; }
    </style>
</head>
<body>
    <h1>Create Building Event</h1>
    <form action="/create-building-event" method="POST">
        <div class="form-group">
            <label for="buildingId">Building ID (auto-generated):</label>
            <input type="text" id="buildingId" name="buildingId" value="<%= buildingId %>" readonly class="readonly-field">
        </div>
        
        <div class="form-group">
            <label for="buildingName">Building Name:</label>
            <input type="text" id="buildingName" name="buildingName" required>
        </div>
        
        <div class="form-group">
            <label for="eventName">Event Name:</label>
            <input type="text" id="eventName" name="eventName" required>
        </div>
        
        <div class="form-group">
            <label for="date">Event Date:</label>
            <input type="datetime-local" id="date" name="date" required>
        </div>
        
        <button type="submit">Create Building Event</button>
    </form>
</body>
</html>
